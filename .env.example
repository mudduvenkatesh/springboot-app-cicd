# Copy to .env and fill in values — NEVER commit .env to git

# App
SPRING_PROFILES_ACTIVE=prod

# Database
DB_URL=jdbc:postgresql://db:5432/appdb
DB_USERNAME=appuser
DB_PASSWORD=changeme
POSTGRES_DB=appdb

# Docker
DOCKER_IMAGE=ghcr.io/YOUR_ORG/springboot-app
IMAGE_TAG=latest
